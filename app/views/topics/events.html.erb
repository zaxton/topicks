<%= render 'topics_list' %>
<div class="topic_form" title="New Topic">
<h3>Start A Convo About Events</h3>
<% form_for(@topic, @topic.category = "Events") do |f| %>
	<%= f.hidden_field :category %>
	<%= f.text_area :content %>
	<%= f.submit "Post" %>
<% end %>
</div>
</div>