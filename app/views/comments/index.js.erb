<% unless @comments.blank? %>
	$('.endless_scroll_inner_wrap').append("<%=escape_javascript(render :partial => 'comments', :collection => @comments)%>");
	$('ul.last').attr('last', '<%=@comments.to_a.last.created_at%>')
<% else %>
	$('#infinite-scroll').detach();
<% end %>